# Celery + broker
celery[redis]>=5.3.0
redis>=5.0.0

# PyTorch – installed separately in Dockerfile with the correct index URL
# (listed here for reference; pip install -r will skip if already installed)

# YOLOv7 runtime dependencies (torch.hub loads yolov7 source automatically)
# Core deps that yolov7 needs:
Pillow>=10.1.0
numpy>=1.26.0
scipy>=1.12.0
opencv-python-headless>=4.9.0
matplotlib>=3.8.0
PyYAML>=6.0
tqdm>=4.66.0
seaborn>=0.13.0
thop>=0.1.1            # FLOPs/params counting used by yolov7
requests>=2.31.0
python-dotenv>=1.0.0
setuptools
